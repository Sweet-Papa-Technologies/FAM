# FAM — Environment Variables
# Copy to .env and fill in values. Never commit .env.
#
# These are used for ${VAR} interpolation in fam.yaml.
# Secrets should NOT go here — use `fam secret set` to store them in the OS keychain.
#
# GOOGLE_CLIENT_ID=
# JIRA_CLIENT_ID=
# FAM_LOG_LEVEL=info
